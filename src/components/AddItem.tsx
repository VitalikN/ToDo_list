import s from "../sass/layouts/addItem.module.scss";
const AddItem = () => {
  return (
    <section className={s.section__add__item}>
      <div className={s.container}>
        <h2 className={s.title}>Add item</h2>
      </div>
    </section>
  );
};
export default AddItem;
